{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../fronend && npm run build && cp dist ../backend ",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin main",
    "dev": "node --watch index.js",
    "start": "node index.js",
    "test": "echo \"Error no test specified\" && exit 1"
  },
  "author": "youssef",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.7.3"
  }
}
